---
version: 3

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-25/ee-minimal-rhel9:latest

dependencies:
  ansible_core:
    package_pip: ansible-core==2.15.13
  ansible_runner:
    package_pip: ansible-runner
  galaxy: 
    collections:
      - kubernetes.core
      - ansible.utils
  python:
    - kubernetes
    - pyyaml
    - jsonpatch
  system:
    - tar
    - gzip
    - util-linux

additional_build_steps:
  prepend_base:
    - RUN microdnf install -y python3-pip tar gzip && microdnf clean all
  
  append_final:
    - |
      RUN curl -L https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux.tar.gz | tar -xvzf - -C /usr/local/bin/ oc && \
      chmod +x /usr/local/bin/oc
    # แก้ไขตรงนี้: ใส่เครื่องหมายคำพูดครอบบรรทัดที่มีเครื่องหมายโคลอน
    - 'RUN echo "Build completed: oc binary and kubernetes dependencies installed"'
    - RUN ansible-galaxy collection list

build_arg_defaults:
  ANSIBLE_GALAXY_CLI_COLLECTION_OPTS: '--ignore-certs'